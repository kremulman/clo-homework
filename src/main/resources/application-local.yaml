spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:homework;MODE=MySQL;
    username: SA
    password:
  jpa:
#    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
